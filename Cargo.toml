[package]
name = "qlings"
version = "0.1.0"
edition = "2021"
description = "Rustlings-style exercises for the q / kdb+ language"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
notify = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[profile.release]
opt-level = 1
