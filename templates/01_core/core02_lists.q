/ core02_lists -- building and taking apart lists

/ A simple list of longs is written with spaces: 10 20 30
/ `count` gives the length, `first` and `last` the ends, `,` joins,
/ and `reverse` flips. `til n` makes 0 1 .. n-1.

/ I AM NOT DONE

/ TODO: the long list 10 20 30
xs:()

/ TODO: how many items are in xs? (use count, not a literal)
n:0N

/ TODO: the first item of xs
lo:0N

/ TODO: the last item of xs
hi:0N

/ TODO: xs with 40 joined on the end
grown:()

/ TODO: xs in reverse order
flipped:()

/ TODO: the first five whole numbers, starting at 0
counted:()
