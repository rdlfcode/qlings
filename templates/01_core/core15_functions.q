/ core15_functions -- lambdas
/
/ A lambda is written in braces. Without a signature it gets the implicit
/ parameters x, y and z:  {x+y}
/ With one, the names are yours: {[a;b] a*b}
/ The last expression is the return value; `:` returns early.
/
/ I AM NOT DONE

/ TODO: a function that doubles its argument
double:{}

/ TODO: a two-argument function returning the larger of x and y
/ (do it with a conditional, not `max`)
larger:{}

/ TODO: a named-parameter function computing a*b using [a;b]
times:{}

/ TODO: a function that returns 0 early for a negative argument and
/       otherwise returns the argument squared
square:{}
